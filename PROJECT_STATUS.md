# حالة مشروع OmniFlow

آخر تحديث: 8 أغسطس 2026

## حالة النسخة المحلية

المنصة تعمل محليًا كمنتج SaaS عربي RTL متعدد الأنشطة، وليست واجهة شكلية. نسخة التطوير تستخدم SQLite لتسهيل المعاينة، ويوجد مخطط PostgreSQL وسياسات RLS وأوامر تطبيق واختبار منفصلة للإنتاج.

- Next.js 16.3 وReact 19 وTypeScript strict وPrisma.
- 60 مسارًا في build الإنتاج.
- `npm run typecheck`: ناجح.
- `npm run lint`: ناجح بلا أخطاء (توجد تحذيرات تنظيف غير حاجبة).
- `npm test`: 14 اختبارًا ناجحًا في 6 ملفات.
- `npm run test:smoke`: ناجح لمسار التسجيل وتأكيد البريد وإنشاء Workspace وDashboard ومنع الوصول بلا جلسة.
- `npm run build`: ناجح.
- `npm audit --omit=dev`: صفر ثغرات معروفة.
- معاينة متصفح فعلية: الصفحة الرئيسية والتسجيل والدخول وRTL والجوال بلا error overlay أو أخطاء console بعد الإصلاح.
- `/api/health`: يعيد 200 ويفحص اتصال قاعدة البيانات، مع تفاصيل محمية عند ضبط `HEALTH_SECRET`.

## ما تم تنفيذه

### الهوية والمصادقة وMulti-Tenant

- تسجيل ودخول وخروج واستعادة كلمة المرور وتأكيد البريد وروابط أحادية الاستخدام.
- جلسات قابلة للإلغاء وcookies آمنة وrate limiting وتدقيق للإجراءات الحساسة.
- Workspaces متعددة للمستخدم، تبديل النشاط، حالات ACTIVE/READ_ONLY/SUSPENDED.
- دعوات فريق وأدوار وصلاحيات وحدود باقات مفروضة في الخادم للعمليات الأساسية.
- مخطط PostgreSQL مولّد وسياسات RLS واختبار عزل A/B وأداة transaction تضبط سياق المستخدم.

### إدارة النشاط

- Dashboard وCRM واستيراد/دمج العملاء والمنتجات والخدمات والطلبات والدفعات والمصاريف والمهام والتقارير.
- فواتير PDF عربية بخط Noto Sans Arabic مضمّن، ضريبة وQR متوافق مع بنية TLV، وتم فحصها بصريًا.
- إعدادات بيانات المنشأة والرقم الضريبي والعنوان.

### Inbox والتكاملات

- Inbox موحد مع pagination وتعيين المحادثة والحالة والملاحظات وإنشاء طلب من المحادثة وتغذية offline/polling.
- اقتراح AI وملخص محادثة وسجل استهلاك وحدود tokens.
- Development Mock وWhatsApp Cloud API وMeta وSMTP adapters.
- Webhooks موقعة وidempotency وطابور durable مع retries وstale-lock recovery وdead-letter.
- تخزين إعدادات القنوات مشفرًا بـAES-256-GCM، إخفاء الأسرار، وفحص اتصال فعلي قبل إظهار «متصل».
- إعداد SMTP المركزي ونماذج AI المتعددة أصبحا قابلين للإدارة والفحص من `/admin/providers` بواسطة Super Admin، مع نموذج افتراضي واحد وتشفير كلمات المرور ومفاتيح API.

### الحملات والأتمتة

- جدولة حملات على الطابور، opt-in/opt-out، suppression، حدود الباقة، idempotency وإحصاءات نجاح/فشل.
- خطوات الأتمتة: إضافة وسم، تعيين عضو، إرسال رسالة، إرسال بريد، ورد AI مع confidence threshold وسجل تشغيل كامل.
- Vercel Cron لمعالجة الوظائف مع `CRON_SECRET`.

### الفريق والفوترة والدعم وSuper Admin

- إدارة الفريق والدعوات والحالات والأدوار.
- صفحة اشتراك للعميل وطلبات دفع يدوي ومراجعة قبول/رفض وتفعيل الاشتراك من Super Admin.
- إدارة الأنشطة والمستخدمين والباقات والأسعار والحدود وهوية الموقع والأسئلة الشائعة والإعلانات والدعم وسجل التدقيق.
- مركز دعم داخل Workspace وإعلانات منصة ظاهرة للمستخدمين.
- مؤشرات queue/webhook/dead-letter/AI في لوحة المدير.

### الجودة والأمان

- Security headers وCSP وcorrelation ID وstructured logs مع حجب مفاتيح الأسرار.
- اختبارات وحدات للمالية والصلاحيات والحدود والتوقيع والتشفير وسياق tenant.
- اختبار Smoke كامل للرحلة الأساسية وتنظيف بياناته المؤقتة.
- README وARCHITECTURE وDATABASE وINTEGRATIONS وSECURITY وملف بيئة بلا أسرار حقيقية.

## بوابات التفعيل الخارجي المتبقية (ليست كودًا محليًا)

هذه البنود لا يمكن إعلان نجاحها قبل توفير حسابات ومفاتيح خارجية حقيقية:

1. **PostgreSQL/RLS الفعلي:** اضبط `POSTGRES_TEST_DATABASE_URL` لقاعدة PostgreSQL اختبارية معزولة ثم شغّل `npm run postgres:rls` و`npm run postgres:test-rls`. لا تستخدم قاعدة الإنتاج للاختبار.
2. **Meta/WhatsApp:** يلزم Meta App ومراجعة الصلاحيات وPage/Phone IDs وtokens وWebhook public HTTPS.
3. **Google/Microsoft OAuth:** يلزم إنشاء التطبيقات وموافقة scopes وredirect URLs. الواجهة تبقى «غير متصل» حتى نجاح الفحص.
4. **SMTP وAI:** أدخل بيانات المزود من `/admin/providers` واضغط فحص الاتصال. لا تستخدم المنصة الإعداد المركزي للإرسال أو التوليد قبل نجاح الفحص، وفشل SMTP لا يمنع إنشاء الحساب في التطوير ويعيد رابط معاينة.
5. **النشر:** اضبط أسرار الإنتاج وPostgreSQL وCron وHTTPS ثم نفّذ فحوص sandbox لكل مزود.

راجع `.env.example` و`INTEGRATIONS.md` و`DATABASE.md` قبل التفعيل الخارجي.
